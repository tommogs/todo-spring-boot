buildscript {
    repositories {
        maven {
            url 'https://plugins.gradle.org/m2/'
        }
    }
    dependencies {
        classpath 'org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:2.6'
    }
}

apply plugin: org.sonarqube.gradle.SonarQubePlugin

sonarqube {
    properties {
        property 'sonar.host.url', 'https://sonarcloud.io'
        property 'sonar.projectKey', 'tommogs_todo-spring-boot'
        property 'sonar.organization', 'tommogs-github'
        property 'sonar.login', '5a10241e0791e11647eb3e1b099901ffaab54c8a'
        properties['sonar.tests'] += sourceSets.integrationTest.java.srcDirs
        
  
    }
}
